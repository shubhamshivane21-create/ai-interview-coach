/**
 * GrainField — subtle dotted texture sitting behind every page.
 * Replaces the neon green grid pattern with a quieter, paper-grain feel
 * that matches the "interview transcript" visual language.
 */
export function GrainField() {
  return <div className="grain-bg" aria-hidden="true" />;
}